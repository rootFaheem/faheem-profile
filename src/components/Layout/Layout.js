import React from 'react';
import classes from './Layout.css';

const layout = (props) => (
    <div className={classes.Layout}>
        <h1 className={classes.Layout}>Hi! I am Faheem :)</h1>
    </div>
);

export default layout;
